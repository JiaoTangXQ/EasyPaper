services:
  backend:
    build: ./backend
    working_dir: /app
    volumes:
      - ./backend/config:/app/config
      - backend-data:/app/data
      - backend-tmp:/app/tmp
      - backend-library:/app/reader-library
    environment:
      - APP_CONFIG_PATH=/app/config/config.yaml
      - EASYPAPER_READER_LIBRARY_DIR=/app/reader-library
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend

volumes:
  backend-data:
  backend-tmp:
  backend-library:
